import { factorial, regressive } from "../src/algorithm"

describe('Binary search', () => {
    test('An orderned ', () => {
        expect(regressive(4))
    })

    test('An orderned ', () => {
        expect(factorial(5)).toBe(120)
    })
})